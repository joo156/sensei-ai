FULL CHANGELOG — Phase 8 backend + frontend, driven by a REAL browser-end-to-end pass
=====================================================================================

What this pass did
  - Ran BOTH apps for real (FastAPI :8000, Vite :8080) and drove the whole flow as a
    real browser would: login -> workspace -> upload -> search -> generate -> review
    -> export. Fixed bugs found ONLY under real running, not in unit tests.

BACKEND   (repo: ai-content-agents)
  backend/auth/jwt.py             (NEW)    encode_hs256 / verify_local_hs256
  backend/auth/supabase.py        (NEW)    GoTrue introspection vs local HS256 strategies
  backend/auth/service.py                  unified authorize flow, returns Bearer token
  backend/config.py                        added SUPABASE_URL / SUPABASE_ANON_KEY /
                                           SUPABASE_JWT_SECRET / LITELLM_* / DEFAULT_MODEL
  backend/deps.py                          auth dependency picks HS256 or GoTrue strategy
  backend/documents/schemas.py   (NEW)     PatchDocumentRequest
  backend/documents/router.py              + PATCH /documents/{document_id}
  backend/documents/service.py             + update_document()  [fixed `tags_json` vs `topics`
                                           column -> was sqlite3.OperationalError no such
                                           column `topics`; corrected to tags_json]
  backend/generation/service.py            + _resolve_model(): bare ids ("mock", vendor w/o
                                           "/") map to DEFAULT_MODEL, applied to 9 call sites,
                                           imported default_model
  backend/review/schemas.py       (NEW)     ReviewItem + GetReviewItemsResponse
  backend/review/service.py       (NEW)     get_review_items_service() (reload-safe queue)
                                           ALSO: catch IllegalTransitionError -> clean 409
                                           illegal_transition (was HTTP 500)
  backend/review/router.py                  + GET /review/items?workspace_id=
  src/validation/review_schema.py           NeedsEdit/Flag transitions + error typing
  tests/*                                   updated to the new auth/export/review shapes

FRONTEND  (Sensei-AI)
----------------------------
  src/api/workspace.api.ts   real mode -> FastAPI (/workspaces CRUD + data + bootstrap)
  src/api/document.api.ts    updateDocument -> PATCH /documents/{id};
                             getDocumentsForWorkspaces -> per-workspace GET /documents
  src/api/review.api.ts      + getReviewItems() (GET /review/items?workspace_id=)
  src/api/paths.ts           + review.items = "/review/items"
  src/api/export.api.ts      (NEW)  export service client
  src/types/api/review.contracts.ts + ReviewItem / GetReviewItemsResponse
  src/routes/review.tsx      real-mode hydration (getReviewItems + audit history), decide() now
                             persists via ReviewService.setState and refreshes backend items/
                             audit; onFlag -> ReviewService.flag; helpers reviewStateFromItem +
                             mergeItems
  src/components/app/BackendStatus.tsx    live backend reachability banner

END-TO-END RESULT (driven live against the running server, local-HS256 JWT)
---------------------------------------------------------------------------
  GET  /workspaces (protected)            -> 200
  POST /workspaces                        -> 201
  POST /upload  (parse->chunk->embed)     -> 201 (multipart, matches browser FormData:
                                               workspace_id WITHOUT filename)
  GET  /documents (library)               -> 200 lists uploaded doc
  PATCH /documents/{id}                   -> 204
  GET  /search?q=<phrase>                 -> 200, multiple queries each return 1 result
                                               ('oxygen','chlorophyll','ATP','chemical energy')
  POST /generate/questions                -> 200, 2 grounded questions, groundingInfo 100%
       (valid LITELLM key)                    (key later returned 401 - external, flaky)
  GET  /review/items                      -> 200, item logged as pending
  POST /review (reject) then (approve)    -> reject 200; approve after reject -> 400/409
                                               clean (was 500 before this pass)
  GET  /audit                             -> 200 shows the decision
  GET  /exports csv (approved)            -> 200 CSV of approved rows only
  GET  /exports (explicit UNapproved id)  -> 403 not_exportable  (export gate verified)

Bug fixes this pass caught and merged
 - Upload 500 "UploadFile not JSON serializable" was caused by the harness, not the
   app; the browser sends workspace_id WITHOUT filename -> works (verified with curl).
 - Review approve-after-reject returned HTTP 500 -> now clean 409 illegal_transition.
 - Generation sending a bare "mock"/model id -> now resolves to DEFAULT_MODEL.
 - Document patch referenced non-existent `topics` column -> corrected to `tags_json`.

Verification
 - Backend: ruff clean; backend boots and all above endpoints exercised live.
 - Frontend: tsc --noEmit clean; npm run lint 0 errors; npm run build ok.

External blockers NOT fixed in source (need real credentials)
 - Supabase anon key was INVALID/REVOKED (401 Invalid API key) -> real browser login
   can't be proven with the committed key. Backend local-HS256 path proven instead.
 - LiteLLM proxy token went from valid (grounding=100) to 401 between runs -> external.
 - Generation grounding guard is content/query dependent -> some texts give
   InsufficientGroundingError surfaced as HTTP 500 (baseline quirk).